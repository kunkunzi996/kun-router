<!--
更新于：V0.8.0 收口与项目洁癖门（2026-07-19）
用途：记录本次 V0.8.0 的真实验收、主分支收口和下一轮入口；不是功能说明的替代品。
-->

# PROJECT_STATE（最小版）

## 项目名称

Kun Coding Router

## 一句话目标

维护一个面向中文新手的 Vibe Coding 项目流程调度 Skill：能判断任务、安排必要关卡、保留安全护栏，并给出可验证的施工与交接路径。

## 当前阶段

V0.8.0「可授权全自动流水线」已完成本地自测、独立真实外部验收、合并 `main`、推送与用户确认的终态分支清理；本轮收口完成。

## 当前目录 / 分支

- 目录：`E:\kun-router`
- 分支：`main`
- 基准分支：`main`

## 启动命令

本项目是纯 Markdown Skill 仓库，没有应用服务或构建产物。

- 自动验收：执行 `计划/V0.8.0-可授权全自动流水线-施工计划书.md` 中列出的 PowerShell 自测。
- 人工验收：按同一计划书的 Mermaid、自动挡路径等清单执行。

## 已完成

- 已读取 README、施工计划书和本轮全部现有目标文件。
- 已确认当前基线为 V0.7.9。
- 已完成本轮强制开工门禁确认：用户选择先建档再施工。
- 已从 `main` 创建并切换到 `feature/v0.8-auto-pipeline`。
- 已新增 19 号流水线收口门，并完成计划列出的 12 个既有文件同步。
- 计划书 9 组 PowerShell 自测、改动范围审计、手动挡原文比对、Markdown 围栏检查和换行符检查均通过。
- 已人工核对计划书的第 5、8 项文档审查：挡位定义有唯一权威位置，Git / worktree 命令标明工作区、暂存边界与清理顺序。
- 已在独立测试仓库完成真实自动挡路径：worktree、同步基准分支、复验、PR、CI、合并、Pages 部署和线上按钮冒烟均通过。
- 已将 `feature/v0.8-auto-pipeline` 快进合并至 `main`，并把提交 `1d83cd1` 推送到 `origin/main`。

## 当前任务

本轮已完成，没有待处理的功能任务；下一轮从 `main` 新建 feature 分支继续。

## 本轮只做

- 新建 `references/19-pipeline-loop.md`。
- 修改计划书列出的 12 个既有文件。
- 运行计划书规定的自动自测，并列出无法在本仓库自动验证的人工验收项。

## 本轮不做

- 不修改 01、02、04-11、14、15、18 号 references 或两个 HANDOFF 模板。
- 不修改 `计划/` 目录。
- 不删除文件或目录。
- 本轮 feature 分支已在用户确认后安全删除；后续分支清理仍须逐项确认。

## MVP 范围

让 Skill 明确区分手动挡与已授权自动挡：自动挡仅在项目流水线契约完整时，使用 feature 分支和 worktree 完成验收后的 commit、同步基准分支、复验、push、PR、CI、合并、部署、线上冒烟和确认后的终态清理。

## 核心验收标准

- 施工计划书列出的 9 组自动自测全部通过。
- 19 号文件不超过 400 行，引用完整且无危险 Git 命令或基准分支硬编码。
- 手动挡原有 Git 行为保持不变；自动挡小改只增加第 6/7 项轻量门禁和 19 收口。
- GitHub Mermaid 渲染与独立测试仓库的自动挡完整路径、CI、部署、线上冒烟均已真实通过。

## 后端骨架验收状态

不适用（纯 Markdown Skill 仓库，无后端目录）。

## 流水线挡位与当前运行状态

- 当前挡位：手动挡（本仓库本轮采用已授权的普通 feature 分支收口；V0.8 自动挡由独立测试仓库真实验收）。
- 自动挡准入：不适用（本仓库未声明完整流水线契约）。
- 状态：`DONE`。
- 已完成：`feature/v0.8-auto-pipeline` → `main`，提交 `1d83cd1fc8bfc819708d061f5ee5f5add76b0c1a` 已推送至 `origin/main`；用户已确认删除同名本地与远端 feature 分支。
- 停止原因：无；本轮终态清理已完成。
- 恢复入口：后续开发从 `main` 新建 feature 分支；不处理 `计划/`。

## 风险 / 待确认

- `计划/` 当前为未跟踪施工图纸，必须原样保留。
- 本仓库没有独立 feature worktree；因此不存在可移除的 worktree 目录。
- 本轮本地与远端 feature 分支均已按用户确认删除。
- 本文件是用户在开工门禁中明确授权的最小建档文件；其余改动仍严格受施工计划书文件清单限制。
- 计划书第 6 组正则默认大小写不敏感，会把安全短参数误判为强删；19 已统一使用等价的 `git branch --delete`，保持安全删除语义并让检查可通过。

## 最近一次 Git 状态

- 基线提交：`4595ad750517fade6db98f000a8f311befe7b914`
- 主分支提交：`1d83cd1fc8bfc819708d061f5ee5f5add76b0c1a`
- 当前分支：`main`，已与 `origin/main` 对齐。
- 未提交内容：仅 `计划/`（用户施工图纸，原样保留）；本轮终态状态将随本次收口提交保存。

## 下一步

如需继续开发，从 `main` 新建下一轮 feature 分支；本轮无需再做分支或 worktree 清理。
