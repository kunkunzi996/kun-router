<!--
更新于：V0.8.2 执行规则收紧收口（2026-07-20）
用途：记录本轮源码状态、真实验收和下一步；不是功能说明的替代品。
-->

# PROJECT_STATE（最小版）

## 项目名称

Kun Coding Router

## 一句话目标

维护一个面向中文新手的 Vibe Coding 项目流程调度 Skill：能判断任务、只启用必要流程、保留安全护栏，并以尽可能低的验证成本给出可信结果。

## 当前阶段

V0.8.2「执行规则收紧」已完成规则修改与本地验收；当前 feature 分支尚未 push 或合并。

## 当前目录 / 分支

- 目录：`E:\kun-router`
- 当前分支：`codex/v0.8.1-router-improvements`
- 基准分支：`main`

## 启动 / 验收方式

本项目是纯 Markdown Skill 仓库，没有应用服务或构建产物。

- 静态验收：检查 frontmatter、Markdown 围栏、reference 路径、位置编号引用、重复模板、文件尺寸和 `git diff --check`。
- 行为验收：用独立 Agent 分别模拟轻量 UI 小改、网页表单验收和未授权桌面跨应用验收。
- 官方 `quick_validate.py`：当前运行时缺少 `PyYAML`，无法启动；已执行等价 frontmatter 结构与字段检查。

## 已完成

- 已从 `main` 创建并使用 `codex/v0.8.1-router-improvements` 分支施工。
- 已把任务调用顺序统一到 16 号，把 Router 报告格式统一到 SKILL.md。
- 已把位置编号改为「自动挡双项轻量门禁」「分支工作区检查」「流水线准入检查」。
- 已把验证分为 V1 终端、V2 浏览器、V3 Computer-Use，并限制 V3 的场景、授权和运行次数。
- 已新增 6 项轻量完成报告，并根据首次前向测试结果补强为强制规则。
- 已把 SKILL、README、FLOWCHART、CHANGELOG 和 PROJECT_STATE 模板收口到 V0.8.1。
- 已通过三个独立前向场景：轻量 UI 使用短报告；网页表单选择 V2、不升 V3；未授权桌面任务拒绝控制电脑并标记「未实际验证」。
- 已收紧后端哨兵：未验收后端目录、本轮后端业务范围和未通过状态必须同时成立；纯前端小改不触发。
- 已统一首次建档、手动挡保存与自动挡收口的精确暂存规则，不再提供可执行的 `git add .`。
- 已让轻量 UI 报告模板只引用 16 号的同名章节，避免与 06 极简规格步骤漂移。
- 已让自动挡同步后用 Git tree 判定是否需要完整复验：无变化沿用初验，有变化重跑完整验收。
- 已通过 V0.8.2 三个独立前向场景：未验收后端的支付 API 直接进入 1 / 2 / 3 暂停模板；手动挡 UI 小改完整列出 16 号表的四步；无代码变化同步在 push 前输出 `REVERIFIED`、tree 证据与下一步。
- 已把 SKILL、README、FLOWCHART、CHANGELOG 和 PROJECT_STATE 模板收口到 V0.8.2。

## 当前任务

V0.8.2 源码与验收记录收口；完成本地提交后，等待用户决定是否 push 或合并。

## 本轮只做

- 收紧后端哨兵触发条件。
- 统一 Git 精确暂存规则。
- 消除轻量 UI 路由漂移。
- 复用无变化同步的验收结果。
- 更新本轮版本与验收记录。

## 本轮不做

- 不修改仓库外文件。
- 不拆分源码仓库与安装目录。
- 不实现语义级重复规则检测。
- 不修改或删除 `计划/` 目录。
- 不 push、不合并、不清理分支，除非用户明确要求。

## MVP 范围

让 Router 的唯一来源清楚、位置引用稳定，并让验证从低成本到高成本逐级升级；后端哨兵不误拦纯前端工作，Git 保存不误带用户文件，自动挡不为无变化同步重复验收。

## 核心验收标准

- 16 号是任务调用顺序唯一来源，SKILL.md 是 Router 报告格式唯一来源。
- 执行规则中不再出现按序号位置引用门禁的写法。
- V1 / V2 足够时禁止使用 V3；V3 必须满足技术必要性和本轮明确授权。
- 轻量路由全绿时必须输出 6 项轻量完成报告；风险升级、V3、未验证或流水线异常时使用完整版。
- 后端哨兵必须同时满足未验收后端存在、本轮后端业务范围和未通过状态；纯前端 UI / 文案不触发。
- 所有挡位的暂存都使用明确路径，并在提交前核对缓存区范围。
- 轻量 UI 的具体步骤只以 16 号同名章节为准。
- 自动挡同步后以 `HEAD^{tree}` 判定：树不变沿用初验，树变化重跑完整验收。
- V0.8.2 三个独立前向场景结果符合上述规则。
- Markdown、reference、frontmatter、尺寸和 Git 空白检查通过。

## 后端骨架验收状态

不适用（纯 Markdown Skill 仓库，无后端目录）。

## 流水线挡位与当前运行状态

- 当前挡位：手动挡。
- 自动挡准入：不适用。
- 状态：本地验收完成，等待用户决定后续 Git 动作。
- 停止原因：无。
- 恢复入口：先读本文件，再核对 `git status` 与 `main..HEAD` 提交。

## 风险 / 待确认

- `计划/` 是用户本机施工图纸，原样保留，并仅写入 `.git/info/exclude`，不修改仓库 `.gitignore`。
- 当前仓库未提供官方 `quick_validate.py`；已执行等价的 frontmatter、Markdown 围栏、reference 路径、暂存命令和 Git 空白检查，但不代表外部验证脚本已实际通过。
- 当前分支尚未 push 或合并。

## 最近一次 Git 状态

- 基线提交：`0461865`（`main`）。
- 当前分支：`codex/v0.8.1-router-improvements`。
- V0.8.1 功能提交：6 个原子提交；另有 1 个版本与验收记录收口提交。
- V0.8.2 功能提交：7 个原子提交；另有 1 个版本与验收记录收口提交。
- 未提交内容：无（`计划/` 仅本机排除，未修改）。

## 下一步

用户确认本轮结果后，再决定是否 push 或合并；分支清理仍需逐项确认。
