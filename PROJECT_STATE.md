<!--
更新于：V0.8.1 路由一致性与验收减负收口（2026-07-20）
用途：记录本轮源码状态、真实验收和下一步；不是功能说明的替代品。
-->

# PROJECT_STATE（最小版）

## 项目名称

Kun Coding Router

## 一句话目标

维护一个面向中文新手的 Vibe Coding 项目流程调度 Skill：能判断任务、只启用必要流程、保留安全护栏，并以尽可能低的验证成本给出可信结果。

## 当前阶段

V0.8.1「路由一致性与验收减负」已完成规则修改、静态检查和三个独立前向场景验收；当前 feature 分支尚未 push 或合并。

## 当前目录 / 分支

- 目录：`E:\kun-router`
- 当前分支：`codex/v0.8.1-router-improvements`
- 基准分支：`main`

## 启动 / 验收方式

本项目是纯 Markdown Skill 仓库，没有应用服务或构建产物。

- 静态验收：检查 frontmatter、Markdown 围栏、reference 路径、位置编号引用、重复模板、文件尺寸和 `git diff --check`。
- 行为验收：用独立 Agent 分别模拟轻量 UI 小改、网页表单验收和未授权桌面跨应用验收。
- 官方 `quick_validate.py`：当前运行时缺少 `PyYAML`，无法启动；已执行等价 frontmatter 结构与字段检查。

## 已完成

- 已从 `main` 创建并使用 `codex/v0.8.1-router-improvements` 分支施工。
- 已把任务调用顺序统一到 16 号，把 Router 报告格式统一到 SKILL.md。
- 已把位置编号改为「自动挡双项轻量门禁」「分支工作区检查」「流水线准入检查」。
- 已把验证分为 V1 终端、V2 浏览器、V3 Computer-Use，并限制 V3 的场景、授权和运行次数。
- 已新增 6 项轻量完成报告，并根据首次前向测试结果补强为强制规则。
- 已把 SKILL、README、FLOWCHART、CHANGELOG 和 PROJECT_STATE 模板收口到 V0.8.1。
- 已通过三个独立前向场景：轻量 UI 使用短报告；网页表单选择 V2、不升 V3；未授权桌面任务拒绝控制电脑并标记「未实际验证」。

## 当前任务

V0.8.1 源码与验收记录收口；完成本地提交后，等待用户决定是否 push 或合并。

## 本轮只做

- 修复路由与报告格式重复。
- 使用语义化门禁名称。
- 分级真实验收并限制 Computer-Use。
- 增加轻量完成报告。
- 更新本轮版本与验收记录。

## 本轮不做

- 不修改仓库外文件。
- 不拆分源码仓库与安装目录。
- 不实现语义级重复规则检测。
- 不修改或删除 `计划/` 目录。
- 不 push、不合并、不清理分支，除非用户明确要求。

## MVP 范围

让 Router 的唯一来源清楚、位置引用稳定，并让验证从低成本到高成本逐级升级；普通网页功能不再默认接管电脑，低风险小改不再输出比改动更长的完成报告。

## 核心验收标准

- 16 号是任务调用顺序唯一来源，SKILL.md 是 Router 报告格式唯一来源。
- 执行规则中不再出现按序号位置引用门禁的写法。
- V1 / V2 足够时禁止使用 V3；V3 必须满足技术必要性和本轮明确授权。
- 轻量路由全绿时必须输出 6 项轻量完成报告；风险升级、V3、未验证或流水线异常时使用完整版。
- 三个独立前向场景结果符合上述规则。
- Markdown、reference、frontmatter、尺寸和 Git 空白检查通过。

## 后端骨架验收状态

不适用（纯 Markdown Skill 仓库，无后端目录）。

## 流水线挡位与当前运行状态

- 当前挡位：手动挡。
- 自动挡准入：不适用。
- 状态：本地验收完成，等待用户决定后续 Git 动作。
- 停止原因：无。
- 恢复入口：先读本文件，再核对 `git status` 与 `main..HEAD` 提交。

## 风险 / 待确认

- `计划/` 是用户未跟踪施工图纸，必须原样保留。
- 官方 `quick_validate.py` 受当前环境缺少 `PyYAML` 阻塞；等价检查能覆盖现有简单 frontmatter，但不代表官方脚本已实际通过。
- 当前分支尚未 push 或合并。

## 最近一次 Git 状态

- 基线提交：`0461865`（`main`）。
- 当前分支：`codex/v0.8.1-router-improvements`。
- 本轮功能提交：6 个原子提交；另有 1 个版本与验收记录收口提交。
- 未提交内容：仅 `计划/`（用户施工图纸，原样保留）。

## 下一步

用户确认本轮结果后，再决定是否 push 或合并；分支清理仍需逐项确认。
